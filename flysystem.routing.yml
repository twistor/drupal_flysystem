flysystem.serve:
  path: '/_flysystem/{scheme}/{filepath}'
  defaults:
    _controller: 'Drupal\system\FileDownloadController::download'
  requirements:
    _access: 'TRUE'
    scheme: '^[a-zA-Z0-9+.-]+$'
    filepath: .+
